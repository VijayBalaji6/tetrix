enum TetrisBlocks { L, J, O, I, T, S, Z ,V}

enum Directions { left, right, down }

Duration gameFrameRate = const Duration(microseconds: 800);

///Screen Grid Dimension
const int rowLength = 10;
const int columnLength = 18;
